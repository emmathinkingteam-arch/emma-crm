# ============================================================
#  Fill these in, then: cp .env.migrate.example .env.migrate
#  NEVER commit .env.migrate (it has secrets).
# ============================================================

# --- OLD project (akgopgdlmnqfklhrpxil, region ap-southeast-1) ---
# Get the password from: Supabase dashboard -> Project Settings -> Database -> Reset/Connection.
# This is the "Session pooler" connection string (IPv4-friendly).
OLD_DB_URL="postgresql://postgres.akgopgdlmnqfklhrpxil:OLD_DB_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres"
OLD_SUPABASE_URL="https://akgopgdlmnqfklhrpxil.supabase.co"
OLD_SERVICE_ROLE_KEY="OLD_SERVICE_ROLE_KEY"   # Settings -> API -> service_role (secret)

# --- NEW project (create it first under the new email) ---
# Replace NEW_REF and NEW_REGION with what the new project shows.
NEW_DB_URL="postgresql://postgres.NEW_REF:NEW_DB_PASSWORD@aws-0-NEW_REGION.pooler.supabase.com:5432/postgres"
NEW_SUPABASE_URL="https://NEW_REF.supabase.co"
NEW_SERVICE_ROLE_KEY="NEW_SERVICE_ROLE_KEY"
